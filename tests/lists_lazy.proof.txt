Reading the language: languages/lists_lazy.lan
{ true } Type { true /\ Inductive(T, int, []) /\ Inductive(T, list, [1]) } 

  preconditions  Value { true /\ Inductive(T, int, []) /\ Inductive(T, list, [1]) /\ Inductive(V, num, []) /\ Inductive(V, nil, []) /\ Inductive(V, cons, []) } 

  preconditions  Context { true /\ Inductive(T, int, []) /\ Inductive(T, list, [1]) /\ Inductive(V, num, []) /\ Inductive(V, nil, []) /\ Inductive(V, cons, []) /\ Inductive(C, head, [1]) } 

  preconditions  Context { true /\ Inductive(T, int, []) /\ Inductive(T, list, [1]) /\ Inductive(V, num, []) /\ Inductive(V, nil, []) /\ Inductive(V, cons, []) /\ Inductive(C, head, [1]) /\ Inductive(C, num, []) /\ Inductive(C, nil, []) /\ Inductive(C, cons, []) } 

 
---- (ITERATE)
 preconditions  T-INT { true /\ Inductive(T, int, []) /\ Inductive(T, list, [1]) /\ Inductive(V, num, []) /\ Inductive(V, nil, []) /\ Inductive(V, cons, []) /\ Inductive(C, head, [1]) /\ Inductive(C, num, []) /\ Inductive(C, nil, []) /\ Inductive(C, cons, []) } 

 
---- (ITERATE)
 preconditions  T-NIL { true /\ Inductive(T, int, []) /\ Inductive(T, list, [1]) /\ Inductive(V, num, []) /\ Inductive(V, nil, []) /\ Inductive(V, cons, []) /\ Inductive(C, head, [1]) /\ Inductive(C, num, []) /\ Inductive(C, nil, []) /\ Inductive(C, cons, []) } 

 
---- (ITERATE)
 preconditions  T-CONS { true /\ Inductive(T, int, []) /\ Inductive(T, list, [1]) /\ Inductive(V, num, []) /\ Inductive(V, nil, []) /\ Inductive(V, cons, []) /\ Inductive(C, head, [1]) /\ Inductive(C, num, []) /\ Inductive(C, nil, []) /\ Inductive(C, cons, []) } 

 
---- (ITERATE)
 preconditions  T-HEAD { true /\ Inductive(T, int, []) /\ Inductive(T, list, [1]) /\ Inductive(V, num, []) /\ Inductive(V, nil, []) /\ Inductive(V, cons, []) /\ Inductive(C, head, [1]) /\ Inductive(C, num, []) /\ Inductive(C, nil, []) /\ Inductive(C, cons, []) } 

 
---- (ITERATE)
 preconditions  R-HEAD { CtxCompliant(R-HEAD) /\ true /\ Inductive(T, int, []) /\ Inductive(T, list, [1]) /\ Inductive(V, num, []) /\ Inductive(V, nil, []) /\ Inductive(V, cons, []) /\ Inductive(C, head, [1]) /\ Inductive(C, num, []) /\ Inductive(C, nil, []) /\ Inductive(C, cons, []) }
---- (LAN)
{ true } (G,I) { CtxCompliant(R-HEAD) /\ true /\ Inductive(T, int, []) /\ Inductive(T, list, [1]) /\ Inductive(V, num, []) /\ Inductive(V, nil, []) /\ Inductive(V, cons, []) /\ Inductive(C, head, [1]) /\ Inductive(C, num, []) /\ Inductive(C, nil, []) /\ Inductive(C, cons, []) }
---- (CONSEQUENCE)
{ true } (G,I) { Inductive(C, cons, []) }

Reading the language: languages/lambda_stlc.lan
{ true } Type { true /\ Inductive(T, int, []) /\ Inductive(T, arrow, [1; 2]) } 

  preconditions  Value { true /\ Inductive(T, int, []) /\ Inductive(T, arrow, [1; 2]) /\ Inductive(V, num, []) /\ Inductive(V, abs, []) } 

  preconditions  Context { true /\ Inductive(T, int, []) /\ Inductive(T, arrow, [1; 2]) /\ Inductive(V, num, []) /\ Inductive(V, abs, []) /\ Inductive(C, app, [1]) /\ Inductive(C, app, [2]) } 

  preconditions  Context { true /\ Inductive(T, int, []) /\ Inductive(T, arrow, [1; 2]) /\ Inductive(V, num, []) /\ Inductive(V, abs, []) /\ Inductive(C, app, [1]) /\ Inductive(C, app, [2]) /\ Inductive(C, num, []) /\ Inductive(C, abs, []) } 

 
---- (ITERATE)
 preconditions  T-INT { true /\ Inductive(T, int, []) /\ Inductive(T, arrow, [1; 2]) /\ Inductive(V, num, []) /\ Inductive(V, abs, []) /\ Inductive(C, app, [1]) /\ Inductive(C, app, [2]) /\ Inductive(C, num, []) /\ Inductive(C, abs, []) } 

 
---- (ITERATE)
 preconditions  T-ABS { true /\ Inductive(T, int, []) /\ Inductive(T, arrow, [1; 2]) /\ Inductive(V, num, []) /\ Inductive(V, abs, []) /\ Inductive(C, app, [1]) /\ Inductive(C, app, [2]) /\ Inductive(C, num, []) /\ Inductive(C, abs, []) } 

 
---- (ITERATE)
 preconditions  T-APP { true /\ Inductive(T, int, []) /\ Inductive(T, arrow, [1; 2]) /\ Inductive(V, num, []) /\ Inductive(V, abs, []) /\ Inductive(C, app, [1]) /\ Inductive(C, app, [2]) /\ Inductive(C, num, []) /\ Inductive(C, abs, []) } 

 
---- (ITERATE)
 preconditions  BETA { CtxCompliant(BETA) /\ true /\ Inductive(T, int, []) /\ Inductive(T, arrow, [1; 2]) /\ Inductive(V, num, []) /\ Inductive(V, abs, []) /\ Inductive(C, app, [1]) /\ Inductive(C, app, [2]) /\ Inductive(C, num, []) /\ Inductive(C, abs, []) }
---- (LAN)
{ true } (G,I) { CtxCompliant(BETA) /\ true /\ Inductive(T, int, []) /\ Inductive(T, arrow, [1; 2]) /\ Inductive(V, num, []) /\ Inductive(V, abs, []) /\ Inductive(C, app, [1]) /\ Inductive(C, app, [2]) /\ Inductive(C, num, []) /\ Inductive(C, abs, []) }
---- (CONSEQUENCE)
{ true } (G,I) { Inductive(T, arrow, [1; 2]) }
